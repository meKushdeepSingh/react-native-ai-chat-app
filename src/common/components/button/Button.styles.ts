import {StyleSheet} from 'react-native';

export const buttonStyles = StyleSheet.create({
  mainView: {
    height: 40,
    width: 40,
    justifyContent: 'center',
    alignItems: 'center',
  },
  hitSlop: {
    top: 10,
    bottom: 10,
    left: 10,
    right: 10,
  },
});
