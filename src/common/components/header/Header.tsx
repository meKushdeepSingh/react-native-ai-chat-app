import React from 'react';
import {View} from 'react-native';
import {headerStyles} from './Header.styles';

const Header: React.FC = () => {
  return <View style={headerStyles.mainView}></View>;
};

export {Header};
